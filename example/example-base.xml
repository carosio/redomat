<?xml version="1.0" encoding="UTF-8"?>
<redo>

	<layer_declaration>

		<remote name="github"
            baseurl="git://github.com/travelping/" />

		<baselayer remote="github"
            revision="deadbeef"
            repo="hello_world.git" />

	</layer_declaration>

	<buildstage id="001-busybox">
		<action>FROM busybox:latest</action>
		<action>RUN mkdir -p /REDO/build</action>
		<action>RUN echo "001-busybox stage done!"</action>
	</buildstage>

</redo>
